import SpaceHeader from "./SpaceHeader";

export default function Space({ spaceId }: { spaceId: string }) {
  // const space = fetch()
  return (
    <div>
      <div>
        <SpaceHeader />
      </div>
    </div>
  );
}
